{
  "buildCommand": "npx convex deploy --cmd 'pnpm run build'",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "functions": {
    "middleware.ts": {
      "runtime": "edge"
    }
  }
}
